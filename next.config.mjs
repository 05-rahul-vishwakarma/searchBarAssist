/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['randomuser.me', 'cdn.example.com', 'images.unsplash.com'], // Add other domains here
    },
};

export default nextConfig;
